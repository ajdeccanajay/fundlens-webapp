# Implementation Plan

- [ ] 1. Write bug condition exploration tests
  - **Property 1: Fault Condition** - Multi-Ticker Rendering Failures (SSE, Citations, Charts, Tables)
  - **CRITICAL**: These tests MUST FAIL on unfixed code - failure confirms the bugs exist
  - **DO NOT attempt to fix the tests or the code when they fail**
  - **NOTE**: These tests encode the expected behavior - they will validate the fixes when they pass after implementation
  - **GOAL**: Surface counterexamples that demonstrate the four bugs exist
  - **Scoped PBT Approach**: Scope properties to concrete failing cases for reproducibility
  - Test implementation details from Fault Condition in design:
    - **Defect 1 - SSE Event Serialization**: Send query "AAPL vs MSFT revenue FY 2023 - 2024", inspect Network tab Response, assert SSE format includes `event: visualization\n` and `event: citations\n` prefixes before `data:` lines
    - **Defect 2 - Metric Citations Missing**: Send structured query, assert citations array populated from metric metadata when narratives array is empty but metrics array has filing information
    - **Defect 3 - Chart Rendering Timing**: Send comparison query with visualization data, assert chart renders successfully even when canvas initially has height 0
    - **Defect 4 - Markdown Table Flushing**: Send query returning markdown table, assert table rendered as complete HTML structure with separator row and all data rows intact
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct - it proves the bugs exist)
  - Document counterexamples found:
    - SSE stream contains only `data: {...}` lines without `event:` prefixes
    - Structured queries return empty citations array despite having metric metadata
    - Chart rendering fails because canvas.offsetHeight === 0 when Chart.js tries to render
    - Markdown tables are flushed mid-structure before separator row arrives
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Single-Ticker Queries, Narrative Citations, Non-Table Markdown, Immediate Canvas Rendering
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Single-ticker queries with semantic retrieval render charts and citations correctly
    - Queries with narrative chunks generate citations from narratives array
    - Non-table markdown (paragraphs, lists, code blocks) flushes at appropriate breakpoints
    - Canvas elements that are immediately visible render charts without delay
    - SSE events without explicit type field are processed as generic data events
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - **Test 1**: For all single-ticker queries with semantic retrieval, charts and citations render exactly as before
    - **Test 2**: For all queries returning narrative chunks, citations extracted from narratives array exactly as before
    - **Test 3**: For all markdown content without tables, flushing occurs at appropriate breakpoints exactly as before
    - **Test 4**: For all visualization events where canvas is immediately visible, charts render without delay exactly as before
    - **Test 5**: For all SSE events with chunk.type === undefined, processed as generic data events exactly as before
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. Fix for multi-ticker rendering failures (SSE, citations, charts, tables)

  - [ ] 3.1 Fix SSE event serialization in controller (most critical)
    - Replace @Sse() decorator with manual SSE streaming using res.write()
    - Remove imports: @Sse(), Sse, MessageEvent, Observable from @nestjs/common and rxjs
    - Add imports: @Res() from @nestjs/common, Response from express
    - Change method signature to async sendMessage(..., @Res() res: Response)
    - Set SSE headers manually: Content-Type: text/event-stream, Cache-Control: no-cache, Connection: keep-alive, X-Accel-Buffering: no
    - Replace subscriber.next() with res.write(\`event: ${chunk.type}\ndata: ${JSON.stringify(chunk.data)}\n\n\`)
    - Add done event: res.write('event: done\ndata: {"complete":true}\n\n') before res.end()
    - Add error handling: catch errors and send event: error\ndata: {...}\n\n before ending stream
    - _Bug_Condition: input.type === 'SSEStream' AND input.hasEventType AND input.serializedFormat NOT CONTAINS 'event: ' + input.eventType_
    - _Expected_Behavior: SSE format includes event: {type}\n before data: {payload}\n line_
    - _Preservation: Generic SSE events with chunk.type === undefined continue to work_
    - _Requirements: 2.1, 3.5_

  - [ ] 3.2 Add metric citation generation in RAG service
    - After building response object in rag.service.ts, check: if ((!citations || citations.length === 0) && metrics.length > 0)
    - Call new helper method: const metricCitations = this.buildMetricCitations(metrics)
    - If metricCitations has items, set citations = metricCitations
    - If answer doesn't contain [1] markers, append source reference section: \n\n**Sources:**\n[1] AAPL 10-K FY2023\n[2] MSFT 10-K FY2023
    - Add buildMetricCitations helper method:
      - Input: metrics: MetricResult[]
      - Output: Citation[] with fields: number, citationNumber, type, sourceType, ticker, filingType, fiscalPeriod, section, excerpt, relevanceScore
      - Use Set to deduplicate by ${ticker}-${filingType}-${fiscalPeriod} key
      - Format excerpt: ${rawLabel}: ${formatValue(value)} (${fiscalPeriod})
      - Assign sequential citation numbers starting from 1
    - Add formatValue helper method:
      - Input: value: number
      - Output: string formatted as currency with appropriate suffix
      - Logic: if abs(value) >= 1e9 return ${(value/1e9).toFixed(1)}B, if >= 1e6 return ${(value/1e6).toFixed(1)}M, if >= 1e3 return ${(value/1e3).toFixed(1)}K, else return ${value.toFixed(2)}
    - _Bug_Condition: input.intentType === 'structured' AND input.narratives.length === 0 AND input.metrics.length > 0 AND input.citations.length === 0_
    - _Expected_Behavior: Citations generated from metric metadata with ticker, filingType, fiscalPeriod_
    - _Preservation: Narrative citation extraction continues to work exactly as before_
    - _Requirements: 2.2, 3.2_

  - [ ] 3.3 Fix chart rendering timing with polling retry
    - In public/app/deals/research.html, replace MutationObserver logic with polling retry loop
    - Remove MutationObserver logic and 5-second timeout
    - Add polling variables: var attempts = 0; var maxAttempts = 20;
    - Create tryRender() function that checks canvas visibility and increments attempts
    - If canvas visible (offsetHeight > 0 && offsetWidth > 0), call doRender(canvas) and return
    - If attempts < maxAttempts, call setTimeout(tryRender, 100) to retry after 100ms
    - If maxAttempts reached, log error: Canvas never became visible after 20 attempts
    - Start polling with initial delay: setTimeout(tryRender, 50) to let Alpine update DOM first
    - Keep existing doRender() function with Chart.js rendering logic unchanged
    - _Bug_Condition: input.hasVisualizationData AND input.canvasElement.offsetHeight === 0_
    - _Expected_Behavior: Chart renders after polling retry finds visible canvas_
    - _Preservation: Immediate canvas rendering continues to work without delay_
    - _Requirements: 2.3, 3.4_

  - [ ] 3.4 Fix markdown table flushing logic
    - In public/app/deals/research.html, improve isMarkdownBreakpoint() table detection
    - Add logic to find last non-empty line: iterate backwards through text.split('\n') to find lastNonEmpty
    - Check if inside table: if (lastNonEmpty.startsWith('|') || lastNonEmpty.endsWith('|'))
    - If inside table, only flush when text ends with |\n\n (table followed by double newline): if (text.endsWith('|\n\n')) return true; else return false;
    - Keep existing flush logic for non-table content: double newline, sentence boundary with lookahead
    - Increase safety valve from 200 to 300 chars to accommodate larger tables: if (text.length - lastFlush > 300)
    - Store last flush position: this._lastMarkdownFlush = text.length
    - _Bug_Condition: input.content.endsWith('|') AND NOT input.content.contains('|---|') AND input.flushTriggered === true_
    - _Expected_Behavior: Table flushed only when text ends with |\n\n (table followed by double newline)_
    - _Preservation: Non-table markdown continues to flush at appropriate breakpoints_
    - _Requirements: 2.4, 3.3_

  - [ ] 3.5 Verify bug condition exploration tests now pass
    - **Property 1: Expected Behavior** - Multi-Ticker Rendering Fixes Validated
    - **IMPORTANT**: Re-run the SAME tests from task 1 - do NOT write new tests
    - The tests from task 1 encode the expected behavior
    - When these tests pass, it confirms the expected behavior is satisfied
    - Run bug condition exploration tests from step 1:
      - SSE serialization test verifies proper event: and data: format
      - Metric citations test verifies citations generated from metric metadata
      - Chart timing test verifies chart renders after polling retry
      - Table flushing test verifies complete table structure rendered
    - **EXPECTED OUTCOME**: Tests PASS (confirms bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [ ] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing Functionality Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2:
      - Single-ticker query test verifies charts and citations render exactly as before
      - Narrative citations test verifies extraction from narratives array exactly as before
      - Non-table markdown test verifies flushing at appropriate breakpoints exactly as before
      - Immediate canvas test verifies chart renders without delay exactly as before
      - Generic SSE events test verifies backward compatibility for chunk.type === undefined exactly as before
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run full test suite including exploration tests and preservation tests
  - Verify all four fixes work correctly:
    - SSE events include proper event: type prefixes
    - Metric citations generated for structured queries
    - Charts render successfully with polling retry
    - Markdown tables rendered as complete structures
  - Verify all preservation requirements met:
    - Single-ticker queries work exactly as before
    - Narrative citations work exactly as before
    - Non-table markdown works exactly as before
    - Immediate canvas rendering works exactly as before
    - Generic SSE events work exactly as before
  - If any tests fail, investigate and fix before proceeding
  - Ask the user if questions arise
